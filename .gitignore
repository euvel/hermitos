# dependencies
node_modules/

# local Wrangler dev + secrets (never commit)
.dev.vars
.wrangler/
.mf/

# os / editor
.DS_Store
*.log
.idea/
.vscode/

# NOTE: public/vm/*.iso, *.wasm, *.bin ARE committed on purpose —
# they are the self-hosted real-Linux image for `boot kernel --real`.
